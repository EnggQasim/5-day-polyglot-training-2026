apiVersion: v2
name: pixelquest
description: A Helm chart for the stateless Pixel Quest API
type: application
version: 0.1.0           # the chart's own version
appVersion: "1.0"       # the app/image version it deploys
